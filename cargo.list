# DEV related tooling I would rather install by cargo than Nixpkgs
cargo-generate --features vendored-openssl
cargo-pio
cargo-update --features vendored-openssl
dioxus-cli --locked
espflash
ldproxy

# Doesn't have Nix package
try-rs

# Nix build is missing aarch64 compatibility
xcp
